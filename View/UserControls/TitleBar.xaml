<!--
Copyright © 2023 antD97
Licensed under the MIT License https://antD.mit-license.org/
-->
<UserControl x:Class="SDPS.View.UserControls.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDPS.View.UserControls"
             mc:Ignorable="d" 
             Height="28" d:DesignWidth="800">
    <Grid MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Background="#2d7d9a">
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Width" Value="28"/>
                <Setter Property="Height" Value="28"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Background" Value="Transparent"/>
            </Style>
        </Grid.Resources>
        
        <TextBlock Text="{Binding Title}" Margin="7,0,0,0" VerticalAlignment="Center"/>
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Click="MinimizeBtn_Click" Visibility="{Binding MinimizeBtnVisibility}" Content="🗕"/>
            <Button Command="{Binding CloseBtnCommand}" Content="✖"/>
        </StackPanel>
    </Grid>
</UserControl>
