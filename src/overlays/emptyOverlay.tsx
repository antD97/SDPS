
function EmptyOverlay() {
  return (
    <div className="flex flex-col items-center justify-center gap-16 bg-neutral-900/95 overflow-hidden">
      <div>This is an empty overlay.</div>
      <div>Select which overlay to use here from the main window.</div>
    </div>
  );
}

export default EmptyOverlay;
